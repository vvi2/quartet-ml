# Neighbour-joining baseline arm: ape::nj() on JC69-corrected distances.
#
# Usage (once implemented):
#   Rscript nj.R <alignment.fasta> <output.nwk>

library(ape)

# TODO: read alignment, compute dist.dna(model = "JC69"), run nj(), write Newick.
